# Set DB_ENGINE=mysql (default) for the primary MySQL implementation,
# or DB_ENGINE=sqlite for a quick local fallback with zero setup.
DB_ENGINE=mysql

DB_HOST=localhost
DB_PORT=3306
DB_NAME=panchayat_street_light
DB_USER=root
DB_PASSWORD=your_password

SECRET_KEY=change-this-to-a-random-value
REPEAT_OFFENDER_MONTHS=12
